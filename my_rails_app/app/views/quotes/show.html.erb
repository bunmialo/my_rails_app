<p>
  <strong>Title:</strong>
  <%= @quote.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @quote.text %>
</p>

<h2>Comments</h2>
<%= render @quote.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_quote_path(@quote) %> |
<%= link_to 'Back', quotes_path %>